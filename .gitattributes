# Enforce LF line endings for all text files (critical: deployed on Linux)
* text=auto eol=lf

# Git LFS
*.zip filter=lfs diff=lfs merge=lfs -text
lab/03_bi_mart_metadata_rag/data/*.csv filter=lfs diff=lfs merge=lfs -text
